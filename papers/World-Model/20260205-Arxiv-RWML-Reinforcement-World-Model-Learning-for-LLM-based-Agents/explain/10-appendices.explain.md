## 说明

- **来源**：原论文 Markdown 中标题为「Appendices（侧车 20260205-Arxiv-Reinforcement-World-Model-Learning-for-LLM-based-Agents-Appendix.md）」的小节（对应标准章节：Appendices (附录)）。
- **解析模板**：`prompts/paper/section-appendices.md`
- **提示摘要**：你是学术论文解读助手。请针对下列 **Appendices（附录）** 原文进行中文解析。

---

### 附录与正文的补充关系

附录为论文正文中的方法提供了详细的实现细节、额外实验数据和补充分析，以支持核心论点。主要内容包括：  
1. **LLM 使用声明（附录A）**：明确 LLM 仅用于论文写作辅助，避免误解其对研究本身的影响。  
2. **基准环境训练细节（附录B、C）**：详述 ALFWorld 和 τ² Bench 两个实验环境的训练设置，包括超参数、数据收集和处理流程。  
3. **消融研究细节（附录D）**：补充 LLM-as-a-judge 评估的实验设计与局限性分析。  
4. **参数变化分析（附录E）**：通过图表和模块分解，深入探讨 RWML 方法的稳定性及知识保留机制。  
这些补充内容共同增强了论文的可复现性与论证深度。

---

### 各附录主要内容分块概括

- **附录A**  
  说明 LLM 仅用于辅助写作（如语法改进），未为研究创意、实验自动化或结果分析作贡献。包含两个表格：  
  - 表A1：比较 RWML 与相关方法在专家动作、LLM 合成数据和成功奖励需求方面的差异。  
  - 表A2：列出 RWML 在 ALFWorld 与 τ² Bench 环境中新引入的超参数（如 τ_d、τ_easy、p），并说明其设置依据。

- **附录B**  
  详述 ALFWorld 环境训练：  
  - B.1：RWML 与 WM SFT 数据收集（Qwen2.5-7B-Instruct 滚动生成轨迹，三元组转换，基于过滤模型的子采样）、训练配置（奖励函数、Prompt设计、硬件配置、超参数）。  
  - B.2：Policy RL 训练设定（遵循前作，使用 GRPO 方法，和正文实验一致）。  
  - B.3：其他方法（如模仿学习、IWM、SR）的训练细节，包括专家数据与反射数据生成。

- **附录C**  
  详述 τ² Bench 环境训练：  
  - C.1：RWML 与 WM SFT 的数据收集（Qwen3-8B 滚动生成，结合多种用户模拟器以提升多样性，对工具响应做掩码处理）、训练配置（自定义奖励函数结合嵌入相似度和 ROUGE 分数、硬件与超参数设置）。  
  - C.2：Policy RL 训练的扩展（支持多轮交互，采用开源用户模拟器降成本）。  
  - C.3：额外评估结果（表A3：官方设置下各方法表现，显示 RWML 竞争力）。  
  - C.4：其他方法训练细节，包括拒绝采样下的专家轨迹收集。

- **附录D**  
  补充 LLM-as-a-judge 评估的消融研究细节：  
  - 提供示例 Prompt（表A8）、给分案例，并指出此自动化评估方法存在给无意义预测高分的问题，因此人工评估仍有必要。

- **附录E**  
  深入分析参数变化：  
  - 以图表展现 ALFWorld 和 τ² Bench 中 Transformer 层及其模块（如注意力投影、MLP）的参数更新比例，对比 RWML、WM SFT 和 Policy RL 的差异。  
  - 结果显示 RWML 更好地保留先验知识、减少参数扰动，为策略优化提供稳定基础。这是实证分析，鼓励未来进一步探索优化动力学。

---

### 对复现或深入阅读最有价值的部分

- **复现最有价值部分**  
  附录 B.1 与 C.1 中 RWML 的训练设置，包括：
  - 数据收集流程（滚动模型、三元组转换、子采样方法）  
  - 超参数取值（表A2 所示 τ_d、τ_easy、p）  
  - 硬件配置（GPU 类型与数量）  
  - 奖励函数实现（如 Qwen3-Embedding-8B 和 ROUGE 分数）  
  此外，附录中模板（表A4-A7）直接给出了训练和评估输入格式，极具参考价值。

- **深入阅读最有价值部分**  
  - 附录E参数变化分析：通过可视化及模块分解详解 RWML 在知识保留和训练稳定性上的机制，有助于理解 RL-based 训练与 SFT 差异及未来研究方向。  
  - 附录C.3额外评估结果（表A3）：补充正文性能对比，显示 RWML 在更严格设置下的表现，增强结论可靠性。  
  - 附录D LLM-as-a-judge 局限性分析：提醒未来采用自动化评估世界模型时需保持警惕。

